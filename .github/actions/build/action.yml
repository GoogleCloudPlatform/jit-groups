name: Build
inputs:
  version:
    required: true
    default: '1.0'
  source-dir:
    required: true
    default: sources  
runs:
  using: "composite"
  steps:
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: 'maven'
        
    - name: Set version
      run: mvn versions:set -DnewVersion=${{ inputs.version }}.$GITHUB_RUN_NUMBER --file ${{ inputs.source-dir }}/pom.xml
      
    - name: Build
      run: mvn -P release -B package -Dmaven.test.skip=true --file ${{ inputs.source-dir }}/pom.xml
